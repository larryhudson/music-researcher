---
import {initDb, executeQuery} from "@src/utils/db";
import Dump from "@src/components/Dump.astro";
const response = executeQuery(`SELECT * from sqlite_master WHERE type='table'`)

initDb();
---

<Dump data={response} />
